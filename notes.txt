have multiple independant lstms maybe with diff hyperparams on the numerial data and average their predictions (maybe discard outliers etc)

cmake --build build --target stock_bot
cmake --build build --target testbed